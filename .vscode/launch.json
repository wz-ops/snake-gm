{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug snake_gm",
      // 这里指向你编译出来的可执行文件路径
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false
      // "terminal": "external"
      // 如果你想在调试前自动运行编译任务，可以取消下面这行的注释
      // "preLaunchTask": "C/C++: g++ build active file"
    }
  ]
}
